import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function RecentJournals() {
  return (
    <Card className="shadow-sm border-slate-200">
      <CardHeader>
        <CardTitle>Recent Journals</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="p-4 text-center text-muted-foreground bg-slate-50 rounded-lg border border-dashed border-slate-200">
          Component Missing
        </div>
      </CardContent>
    </Card>
  );
}
